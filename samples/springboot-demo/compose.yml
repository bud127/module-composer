services:
  combined-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: module-composer/springboot-demo:local
    ports:
      - "8080:8080"
    environment:
      SERVER_ID: combined-local-01
      SERVER_IP: 10.10.1.20
      SERVER_HOSTNAME: combined-docker
    restart: unless-stopped
