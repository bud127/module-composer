# Module Composer Manifesto

We believe modular applications should be composed at build time from explicit module contracts.

We prefer:

- one selected module running directly;
- multiple modules using one generated application;
- explicit configuration imports over broad scanning;
- generated hosts over permanent combined application projects;
- simple Gradle commands over manual dependency editing.
